import controllingAMount from "./controlling-a-mount.md"
import mountedCombatContent from "./mounted-combat.md"
import mountingAndDismounting from "./mounting-and-dismounting.md"

const breadcrumbs = ["Combat", "Mounted Combat"]

const mountedCombat = [
  { title: "Mounted Combat", content: mountedCombatContent, breadcrumbs },
  { title: "Mounting and Dismounting", content: mountingAndDismounting, breadcrumbs },
  { title: "Controlling a Mount", content: controllingAMount, breadcrumbs }
]

export default mountedCombat
